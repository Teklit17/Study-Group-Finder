@model IEnumerable<dynamic>

<h2>Contacts</h2>
<ul>
    @foreach (var user in Model)
    {
    <li>
        <a href="@Url.Action("Chat", "Messages", new { receiverId = user.Id })">@user.UserName</a>
    </li>
    }
</ul>
